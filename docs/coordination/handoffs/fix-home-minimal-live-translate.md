# Handoff: минимальная главная

- Агент: Codex
- Ветка: `main` через отдельный временный worktree
- Commit: этот коммит (SHA фиксируется Git)
- Статус: ready-for-review

## Сделано

- Удалены с главного экрана аналитика, CEFR, метрики, кнопка «Все показатели» и примечание.
- На главной оставлены только название приложения AIBook, кнопка телефонного звонка и карточка Live перевода.
- Удалены лишние `profile` и `cards` props из домашнего компонента, чтобы статистика не могла отрисоваться там случайно.

## Изменённые области

- `components/home/HomeDashboard.tsx` — минимальный экран с двумя действиями.
- `app/page.tsx` — обновлено подключение домашнего компонента.

## Проверки

- `npx eslint components/home/HomeDashboard.tsx` — passed
- `git diff --check` — passed
- `npx next build --webpack` — passed

## Preview

- URL: не создан

## Риски и продолжение

- Старые CSS-классы аналитики оставлены в общей таблице стилей, но больше не используются домашним компонентом; это не влияет на отображение.
- Изменения отправляются поверх актуального `origin/main` без перезаписи параллельных коммитов.
