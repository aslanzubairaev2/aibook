# Handoff: Разнести повторы существительных и подготовить аудио заранее

- Агент: Codex
- Ветка: `fix/noun-article-spaced-queue`
- Commit: `ce91682`
- Статус: ready-for-review

## Сделано

- Повторные задания по одному существительному распределяются по сессии с сохранением порядка режимов для самого слова.
- Та же логика применяется при повторе ошибок. При единственном оставшемся слове пауза между его заданиями физически невозможна.
- Пока ученик отвечает, подготавливается аудио для аудиозаданий среди двух следующих вопросов. Используются существительное без артикля и отдельный кеш `noun-article`.
- Подсказка в настройках больше не обещает показывать все режимы одного слова подряд.

## Изменённые области

- `lib/nounQuizQueue.ts`, `lib/nounQuizQueue.test.ts` — распределение заданий и проверки больших/малых наборов.
- `components/nouns/NounsQuiz.tsx` — очередь и предзагрузка двух следующих вопросов.
- `components/nouns/NounsView.tsx`, `lib/nounsQuizModes.ts` — описание поведения.

## Проверки

- `npm test` — passed, 416 тестов в чистой интеграции с `origin/main` (417 в исходной рабочей ветке с отдельным незакоммиченным тестом).
- `npm run build` — passed в чистой интеграции, включая тесты и TypeScript.
- `npx tsc --noEmit` — passed.
- `npm run lint` — failed на существующем долге проекта: 75 ошибок, 37 предупреждений. Точечный lint изменённых файлов показывает только прежнюю ошибку `react-hooks/set-state-in-effect` в `NounsView.tsx:128`.

## Preview

- URL: отдельный Preview не создавался; владелец попросил интеграцию в `main` для автоматического деплоя подключённого Vercel-проекта.

## Риски и продолжение

- При наборе из одного существительного несколько заданий для него неизбежно окажутся рядом.
- Клиентская предзагрузка работает только для удалённых голосовых провайдеров с доступным Cache API; встроенный голос браузера не требует сетевой подготовки.
