# Handoff: статистика на главном экране

- Агент: Codex
- Ветка: `fix/verb-forms-structured-output` (рабочая директория переключилась на неё параллельным процессом)
- Commit: `e3559c6`
- Статус: ready-for-review

## Сделано

- Главный экран очищен от полок книг, аудиокниг, рекомендаций и карточек действий.
- Оставлены две основные точки входа: `Live перевод` и кнопка голосового звонка с AI.
- Добавлен компактный блок аналитики в стиле референса: CEFR-уровень, прогресс к следующему уровню, словарь, долгосрочная память, точность ответов и завершённые книги.
- Добавлена кнопка `Все показатели` с полноэкранной на мобильном и центрированной на desktop аналитической панелью.
- Подробности панели считаются из существующих карточек/SRS и профиля: интервалы памяти, количество повторённых слов, due-карточки, ответы, книги и минуты чтения.

## Изменённые области

- `components/home/HomeDashboard.tsx` — новый home dashboard и модальная аналитика.
- `app/page.tsx` — передача в home только профиля, карточек и двух действий.
- `styles/globals.css` — стили progress cockpit, метрик и аналитической панели.

## Проверки

- `npx eslint components/home/HomeDashboard.tsx` — passed
- `npx next build --webpack` — passed
- `npm run lint` — failed на существующих ошибках в других файлах ветки (73 errors)
- `npx tsc --noEmit --pretty false` — failed на существующих ошибках в `lib/ai/verbEntryValidation.ts`
- `npm test` — 420 passed, 5 failed; падения вызваны существующим `invalidVerbs is not defined` в `app/api/dictionary/from-image/route.ts`
- `git diff --check` — passed для изменённых областей

## Preview

- URL: не создан; порт 3000 занят уже работающим процессом и локальный запрос не ответил.

## Риски и продолжение

- `Точность ответов` считается как `repetitions / (repetitions + lapses)`, поэтому при отсутствии истории показывается `—`.
- `Завершено книг` использует существующий счётчик `profile.booksFinished`; отдельная статистика завершённых уроков в текущих props home отсутствует.
- Перед merge нужно повторить полный lint/test после интеграции текущих изменений ветки и решить отдельную проблему `invalidVerbs`.
